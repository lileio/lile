package server

import (
	"os"
	"testing"

	"google.golang.org/grpc"

	"{{ .RelDir }}"
	"github.com/lileio/lile/rpc"
)

var s = {{ .CamelCaseName }}Server{}
var cli {{ .SnakeCaseName }}.{{ .CamelCaseName }}Client

func TestMain(m *testing.M) {
	impl := func(g *grpc.Server) {
		{{ .SnakeCaseName }}.Register{{ .CamelCaseName }}Server(g, s)
	}

	ts := rpc.NewRPCServer(rpc.RPCOptions{
		GRPCImplementation: impl,
	})

	addr, serve := rpc.NewTestServer(ts)
	go serve()

	cli = {{ .SnakeCaseName }}.New{{ .CamelCaseName }}Client(rpc.TestConn(addr))

	os.Exit(m.Run())
}
