box: golang:1.8
test:
  steps:
    - setup-go-workspace

    # Gets the dependencies
    - script:
        name: go get
        code: |
          go get

    # Test the project
    - script:
        name: go test
        code: |
          go test ./... -v
